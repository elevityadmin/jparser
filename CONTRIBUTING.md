# Contributing to JParser

1. Fork the project and create a focused branch.
2. Install dependencies with `npm install`.
3. Run `npm run build` before opening a pull request.
4. Explain the user problem and the behavior your change introduces.

Please avoid sending user JSON to external services. Keep pull requests focused and accessible.
